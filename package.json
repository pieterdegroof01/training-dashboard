{
  "name": "training-dashboard",
  "version": "1.0.0",
  "description": "Persoonlijk training en voeding dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd activity-detail && npm ci --include=dev && npm run build",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.3.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
